<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="listener"
            type="com.example.mytestapplication.base.AdapterItemClickListener" />
        <variable
            name="item"
            type="com.example.mytestapplication.memo.data.MemoTile" />
    </data>


    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/clItem"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="@{()->listener.onClickItem(item.id)}"
        android:padding="15dp">

        <TextView
            android:id="@+id/tvMemoTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{item.title}"
            android:textSize="16sp"
            android:textStyle="bold"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            tools:text="@string/app_name"
            />

        <TextView
            android:id="@+id/tvDate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{item.lastUpdate}"
            app:layout_constraintTop_toBottomOf="@id/tvMemoTitle"
            app:layout_constraintStart_toStartOf="parent"
            tools:text="@string/temp"
            />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>